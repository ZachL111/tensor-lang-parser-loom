# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its compilers focus without claiming live deployment or external usage.

## Cases

- `baseline`: `IR pressure`, score 197, lane `ship`
- `stress`: `lowering drift`, score 109, lane `watch`
- `edge`: `stack depth`, score 248, lane `ship`
- `recovery`: `diagnostic reach`, score 208, lane `ship`
- `stale`: `IR pressure`, score 205, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
