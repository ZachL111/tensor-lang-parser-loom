# Field Notes

The useful part of this repository is the small rule set around IR pressure and stack depth.

The domain cases cover `IR pressure`, `lowering drift`, `stack depth`, and `diagnostic reach`. They sit beside the smaller starter fixture so the project has both a compact scoring check and a domain-flavored review check.

The model rewards signal and confidence, then pulls back for drag. In this fixture, `stress` shows where that pullback matters most.

The local verifier covers this data so the notes stay tied to code.
